{
  "name": "good4-community-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project demo-good4-community --only firestore 'node --test rules.test.mjs'"
  },
  "dependencies": { "firebase-admin": "^13.0.0" },
  "devDependencies": { "@firebase/rules-unit-testing": "^4.0.0", "firebase": "^11.0.0", "firebase-tools": "^14.0.0" }
}
